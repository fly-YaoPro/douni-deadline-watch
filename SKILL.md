---
name: douni-deadline-watch
description: 监测豆包用增指定内部群与客户群中的客户要求、同事承诺和长期事项，提取负责人及截止时间，通过飞书交互卡片回收完成状态。用户提到“客户与承诺待办 DDL 提醒”“DDL 预警”“待办汇总”“客户要求”“长期事项”“提醒待办”或要求查看、维护相关卡片时使用；支持原消息定位和 DONE 表情自动完成。
---

# 客户与承诺待办 DDL 提醒

## 适用范围

只读取以下两个已绑定群：

- 内部群：焦尾-豆包用增
- 客户群：豆包专业线-抖音CPE/A/一口价-焦尾

客户群只读；所有汇总和提醒只能发内部群或遥师私聊。不得向客户群回复、发卡片或添加表情。

## 识别规则

读取消息和复合消息的完整语义，登记有明确行动和可验收结果的事项：

- 客户提出的要求、指令和待办。
- 郭子、雅琪及同事提出或承诺完成的工作。
- 有明确 DDL 的短期事项。
- 没有 DDL、但需要持续跟进直到完成的长期事项。

不登记纯汇报、状态同步、内容分享、闲聊、自动提醒或只需知情的消息。同一条消息只登记一条待办；包含多个动作时合并成一个完整事项，不再拆成多张卡。发言人、负责人或待办内容指向豆尼、门神、遥Pro金牌代办等 BOT 的事项不进入人类待办卡，也不计入工作台未完成列表。

发言人、负责人或待办内容涉及“七七”的事项固定排除：新消息不登记，历史未完成记录不进入提醒卡片或工作台列表。底层原始记录仍保留用于审计，不得把排除伪装成“已完成”。

角色口径：胡艺瑛为客户负责人；徐楠、叶文华、周航为主要客户；郭子为内部老板；雅琪/邓雅琪为业务线负责人。其余身份按群边界和企业身份判断，不靠昵称猜测权限。

## 时间与保留

首次启用从上周一开始回填，之后按检查点增量读取。

- 明确到时刻：保存原时刻。
- “ddl 今天 5 点前”等明确相对时刻必须结合原消息发送日期解析为当天 17:00；不得因模型证据包装缺少日期而降级成无 DDL。
- 口语时间结合消息发送时刻消歧。若消息在中午后发出，对方说“今天10点”且没有“上午/早上/凌晨”等限定，应按尚未到来的当天 22:00；明确写了上午时才保留 10:00。
- 只有日期：按当日结束保存，并标记为日期级 DDL。
- 没有时间：保存为长期事项，不虚构 DDL。
- 已登记事项不因超过双周或跨月而删除，底层记录一直保留到完成或人工处理；但提醒有明确止损：有 DDL 的事项到达 DDL 后停止提醒，无 DDL 的事项完成第 3 次正式提醒后停止提醒。停止提醒不等于伪装成已完成。

## 完成信号

以下信号可完成事项：

- 交互卡片回收时至少一名同事点击“已完成”，且当前没有任何“进行中”票。
- 工作台手动完成。
- 原消息出现由同事添加的 `DONE` 表情。

同一事项存在多人协作时，“进行中”优先于“已完成”：只要任一同事当前选择“进行中”，整项继续保留并提醒，不能因另一人点击“已完成”而关闭。每人可在回收前改票，以各自最后一次选择为准。

客户群中的 `DONE` 只有添加者被确认为公司同事时才有效。点赞、OK、收到等表情不等于完成。记录完成信号的人员、时间、消息和来源，供审计和恢复。

## 卡片输出

使用飞书 Card 2.0，标题统一为“客户与承诺待办 DDL 提醒”。提醒按同一事项此前成功发到内部群的正式提醒次数分层；测试卡、遥师私聊预览和手动预览不计次数。

- 首次提醒：保留完整大块，客户要求使用黄色标签，内部要求或同事承诺使用蓝色标签；待办内容和关键信息在左，操作按钮在右，并收集进度。
- 第二次及以后：不再重复大块和操作按钮，统一压成卡片末尾的单行文字，只保留负责人、待办摘要、DDL/提醒次数和原消息入口，不再通过该行收集进度。
- 有 DDL：只要当前时间已经到达 DDL，无论是否完成，都不再进入提醒卡；记录仍保留供审计和人工处理。
- 无 DDL：最多正式提醒 3 次。第 3 次发送后不再进入提醒卡；记录仍保留，可继续通过工作台或原消息 `DONE` 完成。

首次提醒的大块明确展示：

- 说话人
- 负责人；未明确则写“待确认”
- 待办内容
- DDL；无 DDL 写“长期事项”
- 来源群
- “已完成”“进行中”“原消息”三个按钮

负责人能从原消息 @ 或内部人员索引解析到飞书 `open_id` 时，卡片必须使用真实 @ 提醒；多人负责时保存并逐一渲染全部可确认的 `open_id`，不得因为存在多个负责人而把整组 @ 降级成纯姓名。个别负责人无法唯一确认时只显示其姓名，不得猜测或误艾特。

卡片交互窗口由运行时配置控制，默认 30 分钟。只有首次提醒的大块参与本轮回收；重复事项的文字行不产生完成票或进行中票。无人点击或点击“进行中”的首次提醒事项继续保留，并在仍满足提醒条件时于下一轮降级为文字行；完成项不再推送。

单张卡片最多承载 50 条待办，其中首次提醒的大块最多 10 条；重复事项优先合并为卡片末尾文字行。只有首次提醒超过 10 条或总数超过 50 条时才拆卡，减少连续大卡刷屏，同时避免超过飞书组件上限。

## 待办库问答

内部同事询问“DDL 列表/待办清单里有没有某件事”时，必须先查 `client_commitments` 中 tracking v2 的真实待办记录，并结合原消息来源回答：

- 优先匹配用户引号内或明确指出的事项文本；允许忽略空格、标点和大小写，并做高阈值语义近似匹配。
- 返回待办原文、当前状态、负责人、DDL、来源群和可用的原消息链接。
- 已完成记录可说明“已完成、当前不在未完成列表”；未找到则明确写“待办库未找到”。
- 不得为了给出答案，转去机构执行表、达人表或专项表寻找相似选题；更不得把相似业务记录冒充 DDL 待办。
- 只有用户明确询问机构执行进度、达人或专项表时，才允许改走对应表格工具链。

## 与调度分离

本 Skill 只定义业务识别、保留、完成和卡片规则。扫描频率、汇总时间及 DDL 提醒时间由 Daemon 调度台独立配置，不把定时写死在 Skill 中。

夜间不得直接发送 DDL 提醒卡。当前团队工作时段为 10:00–22:00；24 小时触发点落在夜间时，事项保持未提醒状态，由 Daemon 在次日 10:00 自动出队。该门禁只延后发送，不改变原始 DDL、完成状态或 24 小时提醒去重标记。
